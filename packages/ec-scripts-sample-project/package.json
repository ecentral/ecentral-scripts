{
  "name": "ec-scripts-sample-project",
  "version": "1.0.0",
  "description": "A sample project using ec-scripts.",
  "scripts": {
    "init": "cross-env NODE_ENV=development  ec-cli --init",
    "start": "cross-env NODE_ENV=development  ec-cli --start",
    "build": "cross-env NODE_ENV=production  ec-cli --build",
    "build-watch": "cross-env NODE_ENV=production  ec-cli --build --watch",
    "test": "cross-env NODE_ENV=test  ec-cli --test",
    "config": "cross-env NODE_ENV=development  ec-cli --show-config"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "ec-scripts": "git+https://git@gitlab.ecentral.de/f.laukel/ec-scripts.git#develop",
    "ec-scripts-react": "git+https://git@gitlab.ecentral.de/f.laukel/ec-scripts-react.git#develop"
  }
}
